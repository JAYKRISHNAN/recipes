<main role="main">
  <section class="jumbotron text-center">
    <div class="container">
      <h1>Marley Spoon Recipes</h1>
      <p class="lead text-muted">Checkout these cool recipes!</p>
    </div>
  </section>

  <div class="album py-5 bg-light">
    <div class="container">
      <div class="row">
        <%  @recipes.each do |recipe| %>
          <%= render "recipe", recipe: recipe %>
        <% end %>
      </div>
    </div>
  </div>
<main>